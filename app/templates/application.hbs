<PowerSelect
        @searchEnabled={{true}}
        @options={{this.names}}
        @selected={{this.name}}
        @onChange={{action (mut this.name)}}
        @renderInPlace={{true}}
        @verticalPosition="above" as |name|>
  {{name}}
</PowerSelect>